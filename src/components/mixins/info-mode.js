// TODO: drop it. Still accessed by Type Editor.
export default {
  computed: {
    infoMode () {
      return this.mode === 'info'
    },
    formMode () {
      return this.mode === 'form'
    }
  }
}
